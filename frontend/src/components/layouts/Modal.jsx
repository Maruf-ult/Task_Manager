import React from 'react'

const Modal = ({children, isOpen, onClose , title}) => {
  
     if(!isOpen)
          return;
 
     return (
    <div className=''>
      <div className=''>
               
      </div>
     </div>
  )
}

export default Modal